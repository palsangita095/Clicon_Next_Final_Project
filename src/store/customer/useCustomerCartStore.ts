import { useStorefront, Product, CartItem } from "../useStorefront";

export type { Product, CartItem };

export const useCustomerCartStore = useStorefront;
export const useCartStore = useStorefront;
